/**
 * Archivo de entrada para Hostinger (Node.js web app).
 *
 * El panel arranca un `.js` (no Wrangler). Este archivo solo carga el
 * servidor Node que reutiliza el mismo `worker/` de siempre.
 */
import "./project/server/index.js";
